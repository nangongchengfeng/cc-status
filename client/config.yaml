# 中心服务基础地址，程序会向 `${server_url}/api/v1/sync` 发起请求。
server_url: "https://127.0.0.1:8080"

# Bearer Token。生产环境建议改为通过环境变量 `CC_USAGE_CLIENT_AUTH_TOKEN` 注入。
auth_token: "123456"

# 单批上报的最大记录数。当前默认值也是 500。
batch_size: 500

# 单次 HTTP 请求超时时间，单位为秒。
timeout_seconds: 30
